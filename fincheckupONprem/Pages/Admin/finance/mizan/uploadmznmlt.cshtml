@page

@using System.Threading
@using fincheckup.ApiClients.Models.Requests.Finance.Mizan.UploadMznMlt
@using fincheckup.ApiClients.Models.ViewModel
@using fincheckup.ApiClients.Services.Finance
@model IndexModel
@{
    ViewData["Title"] = "Giriş Ekranı";
    Layout = "~/Pages/Shared/_LayoutFinanceMzn.cshtml";
}


@functions {
    public class IndexModel(IFinanceMizanUploadMznMltClient apiClient) : PageModel
    {
        private readonly IFinanceMizanUploadMznMltClient _apiClient = apiClient ?? throw new ArgumentNullException(nameof(apiClient));

        //public IEnumerable<YearResult> myearResult;
        //public long UserID;
        //public long CompID;
        //public int CompCount;
        //public int YearCount;
        //public long curcomID;
        //public int curcomCount;
        //public int Nacenum;        
        //public Company CurrentCom;
        //public HhvnUsers CurrentUser; 
        //public IEnumerable<Entities.Company> mreqListCompany;
        //public string CompName;
        //public string currentcompname { get; set; }

        public MizanUploadMznMltRequestInitialModel InitialModel { get; set; }

        public async Task OnGet()
        {
            try
            {
                var response = await _apiClient.GetAsync(new MizanUploadMznMltOnGetRequest(), CancellationToken.None);

                if (response.IsSuccess)
                {
                    InitialModel = response.Data.InitialModel;
                }
                else
                {
                    Console.WriteLine(response.ProblemDetails.Detail);
                }
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
                throw;
            }

            //myearResult = YearResult.getValue();
            //
            //UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            //CurrentUser = HhvnUsers.GetRow_User(UserID);
            //mreqListCompany = Companies.Getby_User(UserID);
            //CurrentCom = mreqListCompany.Where(x => x.IsDefault == 1).FirstOrDefault();
            //CompName = CurrentCom.CompanyName;
            //currentcompname = CurrentCom.CompanyName;
            //curcomID = CurrentCom.ID;
            //curcomCount = mreqListCompany.Count();
            ////currentUpload = UploadMain.Get_Data(DateTime.Now.Year, curcomID);
            //Nacenum = 80;
            //if (!string.IsNullOrEmpty(CurrentCom.NaceCode) && !string.IsNullOrWhiteSpace(CurrentCom.NaceCode) && CurrentCom.NaceCode.Substring/(0, /2) == "41")
            //{
            //    Nacenum = 15;
            //}
            //CompID = CurrentCom.ID;
            //YearCount = TBLXml.GetYearByComapnyID(CompID);
            //CompCount = mreqListCompany.Count();

        }




        public async Task<JsonResult> OnGetSalerDate(DataSourceLoadOptions options)
        {
            try
            {
                var response = await _apiClient.GetSalerDateAsync(new MizanUploadMznMltOnGetSalerDateRequest
                    {
                        options = options
                    }, CancellationToken.None);

                if (response.IsSuccess)
                {
                    return new JsonResult(response.Data.Response);
                }

                Console.WriteLine(response.ProblemDetails.Detail);
                return new JsonResult("Burada hata var finance/mizan/uploadmznmlt OnGetSalerDate");
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
                throw;
            }

            //UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            //CurrentUser = HhvnUsers.GetRow_User(UserID);
            //
            //curcomID = Companies.Getby_User(UserID).Where(x => x.IsDefault == 1).FirstOrDefault().ID;
            //
            //var currentUploadM = ReportSetMain.Get_StatbyCompanyExcel(curcomID);
            //
            //
            //return new JsonResult(DataSourceLoader.Load(currentUploadM.OrderBy(x => x.MainMonth).ToList(), options));

        }
        public async Task<JsonResult> OnGetSalerYear(DataSourceLoadOptions options)
        {
            try
            {
                var response = await _apiClient.GetSalerYearAsync(new MizanUploadMznMltOnGetSalerYearRequest
                    {
                        options = options
                    }, CancellationToken.None);

                if (response.IsSuccess)
                {
                    return new JsonResult(response.Data.Response);
                }

                Console.WriteLine(response.ProblemDetails.Detail);
                return new JsonResult("Burada hata var finance/mizan/uploadmznmlt OnGetSalerYear");
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
                throw;
            }

            //var YearSetm = YearResult.getValue().OrderByDescending(x => x.MYear);
            //return new JsonResult(DataSourceLoader.Load(YearSetm, options));

        }
        public async Task<JsonResult> OnGetSalerComp(DataSourceLoadOptions options)
        {
            try
            {
                var response = await _apiClient.GetSalerCompAsync(new MizanUploadMznMltOnGetSalerCompRequest
                    {
                        options = options
                    }, CancellationToken.None);

                if (response.IsSuccess)
                {
                    return new JsonResult(response.Data.Response);
                }

                Console.WriteLine(response.ProblemDetails.Detail);
                return new JsonResult("Burada hata var finance/mizan/uploadmznmlt OnGetSalerComp");
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
                throw;
            }

            //UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            //mreqListCompany = Companies.Getby_User(UserID);
            //return new JsonResult(DataSourceLoader.Load(mreqListCompany, options));

        }

        public JsonResult OnGetGraphComp(int ncompid)
        {
            //if (ncompid < 1)
            //{
            //    return new JsonResult("nok");
            //
            //}
            //UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            //if (HhvnUsers.CheckUser(ncompid, UserID))
            //{
            //    UserCompany.Update_UserDefault(UserID, ncompid);
            //}
            //
            //return new JsonResult("ok");
            return new JsonResult("Entegrasyon eksik finance/mizan/uploadmznmlt OnGetGraphComp");
        }
        public JsonResult OnGetGraphYear(int nyear)
        {
            //UserID = Int64.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier));
            //HhvnUsers.SetYear(nyear, UserID);
            //return new JsonResult("ok");
            return new JsonResult("Entegrasyon eksik finance/mizan/uploadmznmlt OnGetGraphYear");
        }

    }
}
@section Styles {
    <script src="~/assets/progress.js"></script>

    <style>
        .dx-rtl .dx-datagrid-rowsview .dx-selection.dx-row > td:not(.dx-focused).dx-datagrid-group-space,
        .dx-rtl .dx-datagrid-rowsview .dx-selection.dx-row:hover > td:not(.dx-focused).dx-datagrid-group-space {
            border-left-color: #bf4e6a;
        }

        .dx-datagrid-rowsview .dx-selection.dx-row:not(.dx-row-lines) > td,
        .dx-datagrid-rowsview .dx-selection.dx-row:hover:not(.dx-row-lines) > td {
            border-bottom: 1px solid #bf4e6a;
            border-top: 1px solid #bf4e6a;
        }

        .dx-datagrid-rowsview .dx-selection.dx-row > td.dx-datagrid-group-space,
        .dx-datagrid-rowsview .dx-selection.dx-row:hover > td.dx-datagrid-group-space {
            border-right-color: #bf4e6a;
        }

        .dx-datagrid-rowsview .dx-selection.dx-row > td,
        .dx-datagrid-rowsview .dx-selection.dx-row:hover > td {
            background-color: #bf4e6a;
            color: #232323;
        }

        .dx-datagrid-table .dx-data-row.dx-state-hover:not(.dx-selection):not(.dx-row-inserted):not(.dx-row-removed):not(.dx-edit-row) > td:not(.dx-focused) {
            background-color: #bf4e6a;
            color: #333;
        }

        .orangecolor {
            background-color: #874651;
        }

        .greencolor {
            background-color: #004aad; 
        }

        /*   .dx-scrollview-content {
                            position: absolute;
                        }*/

        #grdContYearDefter .centerdiv {
            vertical-align: middle !important;
            font-weight: bold;
            font-size: 13px;
            color: whitesmoke;
        }

        #grdContYearDefter .dx-header-row .centerdiv {
            vertical-align: middle !important;
            font-weight: bold;
            font-size: 13px;
            color: black;
        }

        .buttons-demo {
            width: 590px;
            align-self: center;
        }

        .buttons-column > .column-header {
            flex-grow: 0;
            width: 150px;
            height: 5px;
            font-size: 130%;
            opacity: 0.6;
            text-align: left;
            padding-left: 15px;
        }

        .buttons {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

            .buttons > div {
                width: 300px;
                flex-wrap: nowrap;
                display: flex;
            }

        .buttons-column > div {
            padding: 5px;
            width: 190px;
            height: 50px;
            text-align: center;
        }

        .buttons-column {
            width: 190px;
            justify-content: center;
        }
        .dx-toolbar .dx-toolbar-items-container {
            height: 26px;
        }

        .dx-datagrid-group-panel .dx-group-panel-item {
            padding: 3px !important;
        }

        .dx-icon-search {
            width: 17px !important;
            height: 17px !important;
            padding-top: 0px !important;
            padding-bottom: 0px !important;
            margin-top: -9px !important;
        }

        .dx-datagrid-rowsview .dx-row {
            min-height: 47px !important;
            height: 47px !important;
        }

        .dx-texteditor-container .dx-placeholder:before {
            padding-right: unset !important;
            padding-top: unset !important;
            padding-bottom: unset !important;
            padding-left: 34px !important
        }

        .dx-pager {
            padding-top: unset !important;
            padding-bottom: unset !important;
        }  
    </style>
}
<input type="hidden" style="display:none" id="txtcompname" value="@Model.InitialModel.currentcompname" />
<input type="hidden" style="display:none" id="txtcurcomCount" value="@Model.InitialModel.curcomCount" />
<input type="hidden" style="display:none" id="txtcurcomide" value="@Model.InitialModel.curcomID" />
<section class="content home">
    <div class="container-fluid">
        <div class="block-header">
            <div class="row clearfix">
                <div class="col-lg-5 col-md-5 col-sm-12">
                    <h2>Dashboard</h2>
                    <ul class="breadcrumb padding-0">
                        <li class="breadcrumb-item"><a href=""><i class="zmdi zmdi-home"></i></a></li>
                        <li class="breadcrumb-item active">Dashboard</li>
                    </ul>
                </div>
                <div class="col-lg-7 col-md-7 col-sm-12">
                    <div class="input-group m-b-0">
                        <div class="col-12">
                            <div class="card">
                                <div class="body">
                                    <div class="row">
                                        <div class="col-6" style="text-align:left">
                                

                                        </div>
                                        <div class="col-6" style="text-align:right">

                                   
                                            @(Html.DevExtreme().TextBox()
                                                .Value(Model.InitialModel.CompName).ReadOnly(true)
                            )
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="row clearfix">
            <div class="col-lg-6 col-md-6">
                <div class="card">
                    <div class="body l-seagreen text-center">

                        <h3 class="m-b-0 m-t-10 text-white"><span id="txterroredcombin" style="display:none">0</span></h3>
                        <span class="text-white">Yıllara Göre Karşılaştırma Paneli </span>
                    </div>
                </div>
            </div>
            <div class="col-lg-6 col-md-6">
                <div class="card">
                    <div class="body l-seagreen text-center">

                        <div class="buttons-demo">
                            <div class="buttons">
                                <div>

                                 

                                     
                                    <div class="buttons-column">
                                        <div class="column-header">

                                        </div>
                                        <div>
                                            @(Html.DevExtreme().Button()
                        .Text(ButtonStylingMode.Contained.ToString())
                        .Type(ButtonType.Default).Text("Finansal Rapor")
                        .StylingMode(ButtonStylingMode.Contained)
                        .Width(190)
                        .OnClick("notifyf")
                    )
                                        </div>

                                    </div>
                                </div>
                            </div>
                        </div>



                    </div>
                </div>
            </div>

        </div>
        <input type="hidden" style="display:none" id="txtmyear" value="@Model.InitialModel.CurrentUser.SelectedYear" />
        <input type="hidden" style="display:none" id="txtmcomp" value="@Model.InitialModel.CompID" />
        <input type="hidden" style="display:none" id="txtyearcount" value="@Model.InitialModel.YearCount" />
        <input type="hidden" style="display:none" id="txtcompcount" value="@Model.InitialModel.CompCount" />
        <input type="hidden" style="display:none" value="0" id="txtisupdate" />
        <div class="row clearfix">
            <div class="col-lg-12 col-md-12">

                <script>
                    var tyear = $("#txtmyear").val();
                    var mcompid = $("#txtmcomp").val();
                    function onCellPrepared(e) {
                        if (e.columnIndex == 0 && e.rowType == "data") {

                            // e.cellElement.style.backgroundColor = "#73FF33";

                        }
                        if (e.columnIndex == 2 && e.rowType == "data") {
                            var chkk = e.data;
                            console.log(parseInt(chkk.TRowCount));
                            if (parseInt(chkk.TRowCount) < 1) {
                                e.cellElement.find(".dx-command-edit").remove();


                            }
                        }

                        //if (e.columnIndex == 3 && e.rowType == "data") {
                        //    var chkk = e.data;

                        //    if (parseInt(chkk.TRowCount) < 1) {
                        //        e.cellElement.find(".dx-link-edit").remove();


                        //    }
                        //}
                    }
                    var misready = "nok";
                    var chkReadyState = setInterval(function () {
                        if (document.readyState == "complete") {

                            clearInterval(chkReadyState);

                            misready = "ok";
                        }
                    }, 100);
                    function onRowPrepared(info) {
                        if (info.rowType != "header" && info.data.TxResult == "Bekleniyor") {

                            info.rowElement.addClass("orangecolor");
                        } else if (info.rowType != "header") {
                            info.rowElement.addClass("greencolor");
                        }
                    }
                    var dt1 = "0";
                    var compid = $("#txtmcomp").val();
                    var monthid = 0;
                    var year = $("#txtmyear").val();


                    function notifyb() {
                        var tstchk = $("#selectedItemsContainer").val();
                        if (tstchk.length<1) {
                            Swal.fire(
                                'Hata!',
                                'Lütfen Bir vey Daha Fazla Yıl Seçiniz',
                                'error'
                            );
                            return;
                        }
                        else {
                            location.href = "/admin/finance/mizan/dashbilancomlt";
                        }

                    }
                    function notifyg() {
                        var tstchk = $("#selectedItemsContainer").val();
                        if (tstchk.length < 1) {
                            Swal.fire(
                                'Hata!',
                                'Lütfen Bir vey Daha Fazla Yıl Seçiniz',
                                'error'
                            );
                            return;
                        }
                        else {
                            location.href = "/admin/finance/mizan/dashbilancorvnmlt";
                        }
                    }
                    function notifyf() {
                        var tstchk = $("#selectedItemsContainer").val();
                        if (tstchk.length < 1) {
                            Swal.fire(
                                'Hata!',
                                'Lütfen en az iki  veya Daha Fazla Yıl Seçiniz',
                                'error'
                            );
                            return;
                        }
                        else {

                            var words = tstchk.split(",");
                            if (words.length < 2) {
                                Swal.fire(
                                    'Hata!',
                                    'Lütfen en az iki  veya Daha Fazla Yıl Seçiniz',
                                    'error'
                                ); 
                            }
                            else if (words.length > 5) {
                                Swal.fire(
                                    'Hata!',
                                    'En Fazla 5 Yıl Seçilebilmektedir',
                                    'error'
                                );
                            }
                            else {

                                window.open(
                                    "/admin/finance/mizan/reportpotdynamic?nyeara=" + getfdate(),
                                    '_blank' // <- This is what makes it open in a new window.
                                );
                              //  location.href = "/admin/yonetim/reportpotdynamic?nyeara=" + getfdate();
                            }
          
                        }
                    }
                    function getselectYearInstance() {
                        return $("#slctYear").dxSelectBox("instance");
                    }
                    function getselectCompInstance() {
                        return $("#slctCompany").dxSelectBox("instance");
                    }
                    function selectYear_Initialized() {
                        getselectYearInstance().option("value", parseInt($("#txtmyear").val()));
                        var chkco = parseInt($("#txtyearcount").val());

                        //if (chkco < 2) {
                        //    getselectYearInstance().option("readOnly", true);
                        //}


                    }

                    function selectComp_Initialized() {

                        var chkco = parseInt($("#txtcompcount").val());
                        getselectCompInstance().option("value", parseInt($("#txtmcomp").val()));

                        if (chkco < 2) {
                            getselectCompInstance().option("readOnly", true);
                        }
                    }

 
 
                  



                    function startit() {
                        getLoadPanelInstance().show();

                    };
                 

 

                  
                </script>



                <input type="file" id="uploadInput" name="myFile" onchange="readURL(this);" style="display:none" accept=".xls,.xlsx, .csv, .zip" />


                <div class="card">
                    <div class="body">

                        <div class="row">

                            <div class="col-md-12 col-lg-6">
                                <div class="col-md-12 col-lg-12">
                                    <div class="card" id="griddiv" style="display:none">
                                        <div class="header">


                                        </div>
                                        <div class="body">




                                        </div>

                                    </div>
                                </div>

                            </div>
                            <div class="col-md-12 col-lg-6">
                                <div class="row clearfix">
                                    <div class="col-md-12 col-lg-12">


                                        <div class="card">
                                            <div class="body">
                                                <div class="row">
                                                    @(Html.DevExtreme().DataGrid<YearlyErrorResult>
                                                                            ().ColumnHidingEnabled(true).ID("grdContYearDefter").OnCellPrepared("onCellPrepared").OnRowPrepared("onRowPrepared").ShowColumnLines(false).Selection(s => s.Mode(SelectionMode.Multiple).ShowCheckBoxesMode(GridSelectionShowCheckBoxesMode.Always))
    .ColumnMinWidth(50)
    .ColumnAutoWidth(true).ShowBorders(false).OnSelectionChanged("selection_changed")
                                                                            .Height(440) .Columns(columns =>
                                                                            {
                                                                            columns.AddFor(m => m.MainYear).Caption("Yıl").CssClass("centerdiv").SortOrder(SortOrder.Desc) ;
                                                                            columns.AddFor(m => m.MainMonth).Visible(false).SortOrder(SortOrder.Asc); columns.AddFor(m => m.TxResult).Caption("Durum").CssClass("centerdiv");
  
                                                                                columns.AddFor(m => m.TRowCount).Visible(false);

                                                                            }).Toolbar(toolbar =>
                                                                            {
                                                                                toolbar.Items(items =>
                                                                                {


                                                                                    items.Add()
                                                                                        .Location(ToolbarItemLocation.Before)
                                                                                        .Widget(w =>
                                                                                            w.Button()
                                                                                                .Text("Seçimi Temizle")
                                                                                                .Disabled(true)
                                                                                                .OnClick("button_click")
                                                                                                .OnInitialized("button_onInitialized")
                                                                                        );
                                                                                });
                                                                                }).DataSource(d => d.RemoteController().LoadUrl(Url.Page(null, "SalerDate"))
                                                                            ))
                                                </div>

                                                <input type="text" id="selectedItemsContainer" name="selectedItemsContainer" style="display:none" />
                                            </div>
                                        </div>
                                    </div>

                                </div>



                            </div>

                        </div>



                    </div>

                </div>
            </div>

        </div>



    </div>
</section>
@(Html.DevExtreme().LoadPanel()
                    .ID("loadPanel")
                    .ShadingColor("rgba(0,0,0,0.4)")
                    .Position(p => p.Of("#employee"))
                    .Visible(false)
                    .ShowIndicator(true).Message("E-defter İşleniyor.. Lütfen Bekleyiniz...")
                    .ShowPane(true).Height(300).Width(500)
                    .Shading(true)
                    .HideOnOutsideClick(false)
                    .OnShown("loadPanel_shown")
                    )
<input type="text" id="mainyear" name="mainyear" value="@Model.InitialModel.CurrentUser.SelectedYear" style="display:none" />
<input type="text" id="mainmonth" name="mainmonth" style="display:none" />




@section Scripts{
    <script src="~/js/polyfill.min.js"></script>

    <script>
        var changedBySelectBox;
        var titleSelectBox;
        var clearSelectionButton;
        function button_onInitialized(e) {
            clearSelectionButton = e.component;
        }

        function button_click() {
            getDataGridInstance().clearSelection();
        }
        function getDataGridInstance() {
            return $("#grdContYearDefter").dxDataGrid("instance");
        }

        function selection_changed(selectedItems) {
            var dataGrid = getDataGridInstance();
            var data = selectedItems.selectedRowsData;
            clearSelectionButton.option('enabled', true);
            if (data.length > 0) {
                $("#selectedItemsContainer").val(
                    data
                        .map((value) => `${value.MainYear}`)
                        .join(",")
                );
            } else {
                $("#selectedItemsContainer").val("");
            }
            setfdate($("#selectedItemsContainer").val());
            //if (!changedBySelectBox) {
            //    titleSelectBox.option('value', null);
            //}

            //changedBySelectBox = false;
            clearSelectionButton.option('disabled', !data.length);
        }
        function getLoadPanelInstance() {
            return $("#loadPanel").dxLoadPanel("instance");
        }
        function loadPanel_shown(e) {
            setTimeout(function () {
                e.component.hide();
            }, 610000);
        }
         

        var txcompname = $("txtcompname").val();

        function formatMessage(e) {

            return $("#txtcompname").val();
        }

        function setfdate(data) {
            sessionStorage.setItem("fnckpyear", data);

        };
        function getfdate() {
            //
            var usrdate = sessionStorage.getItem("fnckpyear");
            if (usrdate == null || usrdate == undefined || usrdate == "null") {


                usrdate = "";
            }
            else {
                return usrdate;
            }

        };

    </script>

}

